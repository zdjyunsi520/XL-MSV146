var status = -1;

//member of resistance.

function start(mode, type, selection) {
    qm.sendNext("你的第三个任务是保护苏尔。");
    qm.forceStartQuest();
    qm.forceCompleteQuest();
    qm.dispose();
}

function end(mode, type, selection) {
    qm.sendNext("你的第三个任务是保护苏尔。");
    qm.forceStartQuest();
    qm.forceCompleteQuest();
    qm.dispose();
}