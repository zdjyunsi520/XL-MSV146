
var status = 0;

function start() {
    status = -1;
    action(1, 0, 0);
}
// select * from shops order by shopid;
function action(mode, type, selection) {
    cm.openShopNPC(9071001);
    cm.dispose();
}