
var status = 0;

function start() {
    status = -1;
     cm.openShopNPC(105);
     return
    action(1, 0, 0);
}

function action(mode, type, selection) {
    cm.openShopNPC(35);
    cm.dispose();
}