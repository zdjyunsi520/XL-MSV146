function enter(pi) {
    //if (pi.isQuestActive(6141)) { //ninja ambush :D
	//pi.getMap().killAllMonsters(true);
	//pi.spawnMonster(9300088,6); //is it good idea to spawn directly here?
	//pi.playerMessage(5, "暗之领主的门徒出现了。");
    //} else {
    	pi.playerMessage(5, "此传送门当前不可用");
    //}
}